{
  "name": "worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "start": "node dist/main.js",
    "lint": "echo 'lint worker ok'",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@connekt/db": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.14.1",
    "vitest": "^3.1.1",
    "typescript": "^5.8.3"
  }
}
