{
  "name": "connekt-hunter-app",
  "private": true,
  "packageManager": "pnpm@10.11.0",
  "scripts": {
    "db:generate": "pnpm --filter @connekt/db prisma:generate",
    "lint": "turbo lint",
    "pretypecheck": "pnpm db:generate",
    "typecheck": "turbo typecheck",
    "pretest": "pnpm db:generate",
    "test": "turbo test",
    "prebuild": "pnpm db:generate",
    "build": "turbo build"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^5.8.3",
    "eslint": "^9.24.0",
    "@types/node": "^22.14.1",
    "tsx": "^4.19.3",
    "vitest": "^3.1.1"
  }
}
