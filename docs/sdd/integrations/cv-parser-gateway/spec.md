# CV Parser Gateway Spec

- Provider real habilitável por `FF_CV_PARSER_REAL=true`.
- Fallback local mock parser.
- Persistência com confidence por campo em `ResumeParseMetadata`.
- Merge controlado com profile candidato permanece manual/assistido.
