import { SectionCard } from '@connekt-hunter/ui';

export default function DashboardPage() {
  return (
    <SectionCard
      title="Candidate workspace"
      description="Painel inicial com visão resumida da candidatura, próximas ações e status atual."
    />
  );
}
