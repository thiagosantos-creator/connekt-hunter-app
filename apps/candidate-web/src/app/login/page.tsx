import { SectionCard } from '@connekt-hunter/ui';

export default function LoginPage() {
  return (
    <main style={{ padding: '48px', maxWidth: '640px' }}>
      <SectionCard
        title="Login social"
        description="Tela placeholder para login social e recuperação de acesso opcional."
      />
    </main>
  );
}
