# AI Provider Gateway Spec

- Provider real habilitável por `FF_AI_REAL=true`.
- Fallback mock por default.
- Prompt versionado (`promptVersion`) e model versionado (`modelVersion`).
- Logs em `AiExecutionLog`.
- IA apenas assistiva: decisão final sempre humana.
