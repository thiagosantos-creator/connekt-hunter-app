import { SectionCard } from '@connekt-hunter/ui';

export default function Page() {
  return (
    <SectionCard
      title="Entrada por vaga pública"
      description="Listagem contextual da vaga pública e CTA de candidatura."
    />
  );
}
