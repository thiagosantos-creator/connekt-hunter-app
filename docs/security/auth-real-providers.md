# Auth & Security — Real Providers

- Cognito habilitável por `FF_AUTH_REAL` + `COGNITO_*`.
- Sessões locais seguem `UserSession` com provider gravado.
- Hooks de MFA e social login estruturados sem quebra do dev auth.
