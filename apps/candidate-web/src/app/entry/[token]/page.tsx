import { SectionCard } from '@connekt-hunter/ui';

export default function Page() {
  return (
    <SectionCard
      title="Entrada por token"
      description="Validação de token seguro enviado ao candidato."
    />
  );
}
