import { SectionCard } from '@connekt-hunter/ui';

export default function Page() {
  return (
    <SectionCard
      title="Communications"
      description="Área placeholder para templates, dispatches e histórico de mensagens."
    />
  );
}
